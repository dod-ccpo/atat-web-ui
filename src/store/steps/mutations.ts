// import { MutationTree } from "vuex";
// import { StepsState } from "./types";

// const setCurrentStep = (state: StepsState, {stepNumber, stepName}
//     :{stepNumber: string, stepName: string}): void => {
     
//      const step = state.steps.find(stepInfo=> 
//         stepInfo.stepNumber === stepNumber && stepInfo.stepName === stepName );

//      if(step){
//          state.currentStep = step;
//      }
//   };
  

// export const mutations: MutationTree<StepsState> = {

//      [mutationTypes.setCurrentStep] : setCurrentStep,
// }